import Link from 'next/link'
export default function CourseCard({id,title,grade,lessons,progress=0,enrolled=false,price=0}:{id:string,title:string,grade:string,lessons:number,progress?:number,enrolled?:boolean,price?:number}){return <div className="card overflow-hidden"><div className="h-36 bg-[linear-gradient(145deg,#f1e9f8,#fff)] flex items-center justify-center"><div className="w-20 h-20 rounded-3xl bg-[var(--primary-soft)] text-[var(--primary)] grid place-items-center text-3xl font-black">{title.slice(0,1)}</div></div><div className="p-5"><div className="text-sm text-[var(--primary)] font-bold">{grade}</div><h3 className="text-xl font-black mt-1">{title}</h3><div className="flex justify-between text-sm muted mt-4"><span>{lessons} درس</span><span>{enrolled?`${progress}%`:`${price} ج.م`}</span></div>{enrolled?<><div className="h-2 bg-gray-100 rounded-full mt-2 overflow-hidden"><div className="h-full bg-[var(--primary)]" style={{width:`${progress}%`}}/></div><Link href={`/student/lessons`} className="btn btn-primary w-full mt-4 text-center">ابدأ التعلم</Link></>:<Link href={`/checkout/${id}`} className="btn btn-primary w-full mt-4 text-center">احجز الكورس</Link>}</div></div>}
