'use client'; import DashboardShell from '@/components/DashboardShell'; import {useEffect,useState} from 'react'; import Link from 'next/link';
export default function CoursePage({params}:{params:Promise<{id:string}>}){const [d,setD]=useState<any>();const [err,setErr]=useState('');useEffect(()=>{params.then(p=>fetch(`/api/student/courses/${p.id}`).then(async r=>{const x=await r.json();if(!r.ok)throw Error(x.error);setD(x)}).catch(e=>setErr(e.message)))},[params]);return <DashboardShell title="الكورس"><Link href="/student/courses" className="text-[var(--primary)] font-bold">← الكورسات</Link>{err?<div className="card p-8 mt-5 text-center">{err}</div>:!d?<div className="card p-10 mt-5 animate-pulse">جاري تحميل الكورس...</div>:<div className="mt-5"><div className="card p-7"><span className="badge">{d.course.grade} • {d.course.subject}</span><h1 className="text-3xl font-black mt-3">{d.course.title}</h1><p className="muted mt-2">مع {d.course.teacher}</p><p className="mt-5 leading-8">{d.course.description}</p><div className="mt-6 flex items-center justify-between gap-4"><div><div className="muted text-sm">سعر الكورس</div><div className="text-2xl font-black">{d.course.price} ج.م</div></div><Link href={`/checkout/${d.course.id}`} className="btn btn-primary">احجز الكورس</Link></div></div><div className="space-y-4 mt-6">{d.course.modules.map((m:any)=><div key={m.id} className="card overflow-hidden"><div className="p-5 bg-[var(--primary-soft)]"><h2 className="font-black text-xl">{m.title}</h2></div><div>{m.lessons.map((l:any)=><Link key={l.id} href={`/student/lessons/${l.id}`} className="flex items-center justify-between p-5 border-t hover:bg-gray-50"><div><b>{l.order}. {l.title}</b><div className="text-sm muted mt-1">{l.hasVideo?'فيديو':'محتوى نصي'} • {l.completed?'مكتمل':`${Math.round(l.progress)}%`}</div></div><span className="badge">{l.completed?'✓ مكتمل':'فتح الدرس'}</span></Link>)}</div></div>)}</div></div>}</DashboardShell>}
