'use client'; import DashboardShell from '@/components/DashboardShell'; import {useEffect,useState} from 'react';
export default function Books(){const [d,setD]=useState<any>();useEffect(()=>{fetch('/api/student/books').then(r=>r.json()).then(setD)},[]);return <DashboardShell title="الكتب"><h1 className="text-3xl font-black">كتبك الرقمية</h1>{!d?<div className="card p-10 mt-6 animate-pulse">جاري التحميل...</div>:<div className="grid md:grid-cols-2 lg:grid-cols-3 gap-5 mt-7">{d.books.map((b:any)=><div className="card p-6" key={b.id}><div className="w-16 h-20 rounded-xl bg-[var(--primary-soft)] grid place-items-center text-2xl">📘</div><h2 className="font-black text-xl mt-4">{b.title}</h2><p className="muted mt-2">{b.grade?.name} • {b.subject?.name}</p>{b.fileUrl?<a className="btn btn-primary mt-5 inline-flex" href={b.fileUrl} target="_blank">فتح الكتاب</a>:<span className="badge mt-5 inline-flex">سيتم إضافة الملف قريبًا</span>}</div>)}{!d.books.length&&<div className="card p-8 muted">لا توجد كتب متاحة لصفك حاليًا.</div>}</div>}</DashboardShell>}
