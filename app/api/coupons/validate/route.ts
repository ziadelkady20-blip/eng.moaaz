import {NextResponse} from 'next/server'; import {db} from '@/lib/db'; import {getCurrentUser} from '@/lib/auth'; import {z} from 'zod';
const schema=z.object({code:z.string().min(1),courseId:z.string()});
export async function POST(req:Request){const user=await getCurrentUser(); if(!user?.student)return NextResponse.json({error:'سجل دخولك كطالب أولًا'},{status:401}); try{const {code,courseId}=schema.parse(await req.json());const c=await db.coupon.findUnique({where:{code:code.toUpperCase()}});const now=new Date();if(!c||!c.active||(c.startsAt&&c.startsAt>now)||(c.endsAt&&c.endsAt<now)||c.usageLimit&&c.usedCount>=c.usageLimit)return NextResponse.json({error:'كود الخصم غير صالح أو منتهي'},{status:400});if(c.courseId&&c.courseId!==courseId)return NextResponse.json({error:'الكود غير متاح لهذا الكورس'},{status:400});const course=await db.course.findUnique({where:{id:courseId}});if(!course)return NextResponse.json({error:'الكورس غير موجود'},{status:404});const discount=c.type==='PERCENT'?Math.min(course.price,course.price*c.value/100):Math.min(course.price,c.value);return NextResponse.json({valid:true,discount});}catch{return NextResponse.json({error:'بيانات غير صالحة'},{status:400})}}
