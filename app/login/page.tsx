'use client'
import Link from 'next/link'; import {useState} from 'react'; import {useRouter} from 'next/navigation'
export default function Login(){const router=useRouter();const [phone,setPhone]=useState('');const [password,setPassword]=useState('');const [loading,setLoading]=useState(false);const [error,setError]=useState('');
async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');try{const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({phone,password})});const d=await r.json();if(!r.ok)throw new Error(d.error);const target=d.user.role==='ADMIN'?'/admin':d.user.role==='TEACHER'?'/teacher':d.user.role==='PARENT'?'/parent':'/student';router.push(target);router.refresh()}catch(e){setError(e instanceof Error?e.message:'حدث خطأ غير متوقع')}finally{setLoading(false)}}
return <main className="min-h-screen grid place-items-center p-5"><div className="card w-full max-w-md p-8"><div className="flex items-center gap-3"><img src="/brand-logo.png" className="w-20 h-12 object-cover rounded-xl" alt="شعار Eng Moaaz Ismail"/><div><div className="text-2xl font-black text-[var(--primary)]">تسجيل الدخول</div><p className="muted text-xs mt-1">Eng Moaaz Ismail • الثانوية العامة</p></div></div><form onSubmit={submit} className="space-y-4 mt-7"><label className="block font-bold">رقم الهاتف<input required value={phone} onChange={e=>setPhone(e.target.value)} inputMode="tel" className="w-full mt-2 border rounded-xl p-3" placeholder="01xxxxxxxxx"/></label><label className="block font-bold">كلمة المرور<input required value={password} onChange={e=>setPassword(e.target.value)} type="password" className="w-full mt-2 border rounded-xl p-3" placeholder="••••••••"/></label>{error&&<div className="rounded-xl bg-red-50 text-red-700 p-3 text-sm font-bold">{error}</div>}<button disabled={loading} className="btn btn-primary w-full disabled:opacity-60">{loading?'جاري الدخول...':'دخول'}</button></form><div className="flex justify-between text-sm mt-5"><Link className="text-[var(--primary)] font-bold" href="/register">إنشاء حساب</Link><span className="text-[var(--primary)] font-bold">نسيت كلمة المرور؟</span></div></div></main>}
