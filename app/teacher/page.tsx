'use client';import DashboardShell from '@/components/DashboardShell';import Stat from '@/components/ui/Stat';import Link from 'next/link';import{useEffect,useState}from'react';
export default function Teacher(){const[d,setD]=useState<any>();useEffect(()=>{fetch('/api/teacher/dashboard').then(r=>r.json()).then(setD)},[]);if(!d)return <DashboardShell title="لوحة المدرس"><div className="card p-10">جاري تحميل لوحة المدرس...</div></DashboardShell>;return <DashboardShell title="لوحة المدرس"><h1 className="text-3xl font-black">مرحبًا يا {d.teacher?.split(' ')[0]||'أستاذنا'} 👋</h1><p className="muted mt-2 mb-7">إدارة المحتوى والطلاب والواجبات من مكان واحد.</p><div className="grid sm:grid-cols-2 lg:grid-cols-4 gap-4"><Stat label="الكورسات" value={d.stats.courses}/><Stat label="الطلاب" value={d.stats.students}/><Stat label="الدروس" value={d.stats.lessons}/><Stat label="واجبات تحتاج مراجعة" value={d.stats.pendingAssignments}/></div><div className="flex flex-wrap gap-3 mt-8"><Link className="btn btn-primary" href="/teacher/courses">إدارة الكورسات</Link><Link className="btn btn-secondary" href="/teacher/students">الطلاب</Link><Link className="btn btn-secondary" href="/teacher/assignments">الواجبات</Link><Link className="btn btn-secondary" href="/teacher/attendance">الحضور</Link></div><h2 className="text-2xl font-black mt-9 mb-4">كورساتك</h2><div className="grid md:grid-cols-2 lg:grid-cols-3 gap-5">{d.courses.map((c:any)=><Link key={c.id} href={`/teacher/courses/${c.id}`} className="card p-6 hover:-translate-y-1 transition"><div className="flex justify-between"><span className={`badge ${c.published?'badge-success':'badge-muted'}`}>{c.published?'منشور':'مسودة'}</span><span className="text-2xl">📚</span></div><h3 className="font-black text-xl mt-5">{c.title}</h3><p className="muted mt-2">{c.students} طالب • {c.lessons} درس</p></Link>)}</div></DashboardShell>}
