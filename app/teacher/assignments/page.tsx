'use client';import DashboardShell from '@/components/DashboardShell';import{useEffect,useState}from'react';export default function Assignments(){const[d,setD]=useState<any[]>([]);useEffect(()=>{fetch('/api/teacher/assignments').then(r=>r.json()).then(setD)},[]);return <DashboardShell title="الواجبات"><h1 className="text-3xl font-black">الواجبات والتسليمات</h1><div className="grid md:grid-cols-2 gap-5 mt-6">{d.map(a=><div key={a.id} className="card p-6"><h2 className="font-black text-xl">{a.title}</h2><p className="muted mt-1">{a.course.title}</p><div className="mt-5"><b>{a.submissions.length}</b> تسليم</div><div className="mt-4 space-y-2">{a.submissions.slice(0,5).map((s:any)=><div key={s.id} className="p-3 rounded-xl bg-[var(--surface-soft)] flex justify-between"><span>{s.student.user.name}</span><span>{s.score==null?'يحتاج تصحيح':`${s.score}%`}</span></div>)}</div></div>)}{!d.length&&<div className="card p-8 muted">لا توجد واجبات حاليًا.</div>}</div></DashboardShell>}
