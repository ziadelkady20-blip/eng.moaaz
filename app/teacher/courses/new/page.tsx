'use client';import{useEffect,useState}from'react';import{useRouter}from'next/navigation';import DashboardShell from'@/components/DashboardShell';export default function NewCourse(){const r=useRouter();const[m,setM]=useState<any>({grades:[],subjects:[]});const[f,setF]=useState({title:'',description:'',price:'0',gradeId:'',subjectId:''});const[error,setError]=useState('');useEffect(()=>{fetch('/api/admin/meta').then(x=>x.json()).then(setM)},[]);async function save(e:any){e.preventDefault();setError('');const res=await fetch('/api/teacher/courses',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...f,price:Number(f.price),published:false})});const d=await res.json();if(!res.ok)return setError(d.error||'تعذر إنشاء الكورس');r.push('/teacher/courses')}return <DashboardShell title="إنشاء كورس"><form onSubmit={save} className="card p-7 max-w-2xl space-y-4"><h1 className="text-2xl font-black">إنشاء كورس جديد</h1><input className="input" required placeholder="اسم الكورس" value={f.title} onChange={e=>setF({...f,title:e.target.value})}/><textarea className="input min-h-28" placeholder="وصف الكورس" value={f.description} onChange={e=>setF({...f,description:e.target.value})}/><input className="input" type="number" min="0" step="0.01" placeholder="السعر" value={f.price} onChange={e=>setF({...f,price:e.target.value})}/><select className="input" required value={f.gradeId} onChange={e=>setF({...f,gradeId:e.target.value})}><option value="">الصف</option>{(m.grades||[]).map((x:any)=><option key={x.id} value={x.id}>{x.name}</option>)}</select><select className="input" required value={f.subjectId} onChange={e=>setF({...f,subjectId:e.target.value})}><option value="">المادة</option>{(m.subjects||[]).map((x:any)=><option key={x.id} value={x.id}>{x.name}</option>)}</select>{error&&<div className="text-red-600 font-bold">{error}</div>}<button className="btn btn-primary w-full">إنشاء الكورس</button></form></DashboardShell>}
