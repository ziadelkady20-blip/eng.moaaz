'use client';import DashboardShell from '@/components/DashboardShell';import Link from 'next/link';import{useEffect,useState}from'react';export default function Courses(){const[d,setD]=useState<any[]>([]);useEffect(()=>{fetch('/api/teacher/courses').then(r=>r.json()).then(setD)},[]);return <DashboardShell title="إدارة الكورسات"><div className="flex justify-between items-center"><div><h1 className="text-3xl font-black">إدارة الكورسات</h1><p className="muted mt-2">أضف الدروس واربط فيديوهات YouTube بالمحتوى.</p></div><Link href="/teacher/courses/new" className="btn btn-primary">+ كورس جديد</Link></div><div className="grid md:grid-cols-2 lg:grid-cols-3 gap-5 mt-7">{d.map(c=><Link href={`/teacher/courses/${c.id}`} key={c.id} className="card p-6 hover:-translate-y-1 transition"><span className="badge">{c.grade?.name}</span><h2 className="font-black text-xl mt-4">{c.title}</h2><p className="muted mt-2">{c.modules.length} وحدة • {c.modules.reduce((a:any,m:any)=>a+m.lessons.length,0)} درس</p></Link>)}</div></DashboardShell>}
