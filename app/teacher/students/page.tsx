'use client';import DashboardShell from '@/components/DashboardShell';import{useEffect,useState}from'react';export default function Students(){const[d,setD]=useState<any[]>([]);useEffect(()=>{fetch('/api/teacher/students').then(r=>r.json()).then(setD)},[]);return <DashboardShell title="الطلاب"><h1 className="text-3xl font-black">طلابك</h1><div className="card mt-6 overflow-hidden"><div className="overflow-x-auto"><table className="w-full text-right"><thead><tr className="border-b"><th className="p-4">الطالب</th><th>الصف</th><th>الحضور</th><th>المتوسط</th><th>الكورسات</th></tr></thead><tbody>{d.map(s=><tr key={s.id} className="border-b last:border-0"><td className="p-4 font-bold">{s.name}</td><td>{s.grade}</td><td>{s.attendance}%</td><td>{s.avg}%</td><td>{s.courses.length}</td></tr>)}</tbody></table>{!d.length&&<p className="p-8 muted">لا يوجد طلاب مرتبطون بكورساتك.</p>}</div></div></DashboardShell>}
