'use client'
import AdminShell from '@/components/AdminShell'; import {useEffect,useState} from 'react'
export default function Users(){const [users,setUsers]=useState<any[]>([]);const [form,setForm]=useState({name:'',phone:'',password:'',role:'STUDENT'});const load=()=>fetch('/api/admin/users').then(r=>r.json()).then(d=>setUsers(d.users||[]));useEffect(()=>{load()},[]);async function add(e:any){e.preventDefault();const r=await fetch('/api/admin/users',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const d=await r.json();if(!r.ok)return alert(d.error);setForm({name:'',phone:'',password:'',role:'STUDENT'});load()}return <AdminShell><h1 className="text-3xl font-black">المستخدمون</h1><p className="muted mt-2 mb-7">إدارة حسابات الطلاب والمدرسين والدعم. إنشاء حسابات ADMIN متاح فقط للسوبر أدمن.</p><div className="grid lg:grid-cols-[360px_1fr] gap-6"><form onSubmit={add} className="card p-6 space-y-3"><h2 className="font-black text-xl">إضافة مستخدم</h2><input required className="w-full border rounded-xl p-3" placeholder="الاسم" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input required className="w-full border rounded-xl p-3" placeholder="رقم الهاتف" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/><input required minLength={8} type="password" className="w-full border rounded-xl p-3" placeholder="كلمة المرور" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><select className="w-full border rounded-xl p-3" value={form.role} onChange={e=>setForm({...form,role:e.target.value})}>{[['STUDENT','طالب'],['PARENT','ولي أمر'],['TEACHER','مدرس'],['SUPPORT','دعم']].map(x=><option key={x[0]} value={x[0]}>{x[1]}</option>)}</select><button className="btn btn-primary w-full">إنشاء الحساب</button></form><div className="card p-4 overflow-auto"><table className="w-full text-right"><thead><tr className="border-b"><th className="p-3">الاسم</th><th>الهاتف</th><th>الدور</th><th>الصف</th><th>التاريخ</th></tr></thead><tbody>{users.map(u=><tr key={u.id} className="border-b"><td className="p-3 font-bold">{u.name}</td><td>{u.phone}</td><td><span className="badge">{u.role}</span></td><td>{u.student?.grade?.name||'—'}</td><td>{new Date(u.createdAt).toLocaleDateString('ar-EG')}</td></tr>)}</tbody></table></div></div></AdminShell>}
